{
    "require-dev": {
        "phpunit/phpunit": "11.0.*",
        "phpstan/phpstan": "1.10.*"
    },
    "scripts": {
        "test": "./vendor/bin/phpunit test",
        "cs": "./vendor/bin/phpstan analyse"
    }
}
